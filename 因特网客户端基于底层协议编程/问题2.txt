1.上传文件时，ftplib.storbinary('STOR %s' % filepath, fp.read())这个用法要求filepath是原生字符串的那种路径，
当你的路径中有'\t',或是'\n'这种怎么处理。
答1:理解有误。print出的字符串是\其实它本身是\\存储的，比如把它存到列表中输出按个列表就可以“清楚”看到了。
2.书签存在哪里？怎么存比较安全？---（记录访问过的ftp网址和登录信息）
答2:安全就先不考虑了，因为即使是使用了什么加密算法，如果这个客户端的代码被不法分子取到也是可以反着破解的（个人看法）
书签可以存在os.curdir+\bookmark.txt中。方法是pickle库的dump和load方法实现。
3.历史记录包含什么内容，放到哪里？
答3:上传和下载的文件记录，包括文件名，时间，从哪个路径到哪条路径（具体）三条。把他放到py脚本所在目录（经过串行化之后）。
4.下载进度显示
答4:靠os.path.getsize(ab_path)实时获取已下载的字节数，先做那种数字式的更新的显示，再考虑进度条式。
    追问4.1 对于多个同类型的文件，下载线程和显示进度线程会出现对不上的情况导致出错
    4.2    对于多个同类型的文件，下载完成以后会出现进度线程无限循环不关闭的问题
    答4.1（追）:还想着让两个线程之间来回通信，觉得他们的输出好像有点混乱，就尝试性的加了个锁，混乱问题就解决了，看来是输出时发生了抢占。
    答4.2无限循环的问题，我还想不明白，因为进度线程应该是按逻辑走的话--若下载先下好了，那它就跳到显示下一个的进度里，
    最终全部的进度都是100%它就理应自动关闭了，这个锁不知道怎么把它也搞定了。
···
进度显示对于50MB的文件如果是延迟0.1s,什么都看不到，直接就是完成了。（0.05s还是可以看到一点进度的变化的）
···
5.上传进度显示
答5:类似第四个，用os.size(filename)实时获取已上传的字节数。今天想先放一放。