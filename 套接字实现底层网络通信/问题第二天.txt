1.服务器连接到多个用户之后怎样区别不同的信息来源?---靠的是它连接后新建的socket，该对象可以socket.recv返回 (bytes, address) 元组
用户间通信我的技术水平：信息还得通过服务器转送---简单实现
b 方案：
用户：发送信息前要写个接收者的“前缀”
服务器：接收到一个用户的信息之后，定向发送给他想发送的另一个用户

a 方案：（在用户之间建立连接，然后全双工）
用户1想和用户2聊天，用户1把请求给服务器（省略细节）；
#用户连接
    服务器创建个线程---把用户2的地址（主机名IP,端口PORT）给用户1---用户1创建线程并用该地址通过creat_connection（）
得到通信所需的套接字对象，用于接受、发送消息（用户1这边和用户2聊天的房间）。同理服务器的线程也把用户1的地址给用户2同理做法。
#双全工的实现，及时性等等问题
