1.连接好以后，客户端给服务器的输入，不会被服务器立即输出。当服务器输出的时候，它才会显示已经收到的内容。
---如果给客户端一个time.sleep(2)的短暂的等待的话会受到服务器的第一个内容的
2.需要按奇怪的顺序输入，且输入内容不能及时显示
---是input()的阻塞效果
3.最后竟然都不能输入
---使用线程同步的锁（最低级的同步控制）
4.IDLE环境下打开两个窗体，之间的及时交互（一个发消息另一个不会立即收到并显示），在这上面改了很多代码，发现同样的代码在命令行下可以正确执行（可以及时交互出来）。但是在之前版本的半双工就没有出现问题。可能是当时没用线程来写。
5.半双工（就是一人发送，在他想下次发之前得等对面传来的信息）代码被我改没了，很好做，因为他的执行顺序固定。